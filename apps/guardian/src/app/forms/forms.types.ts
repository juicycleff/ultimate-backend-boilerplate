import { ObjectType } from '@nestjs/graphql';

/**
 * @description UI schema for auth and registration flow
 */
@ObjectType({ description: 'UI schema for auth and registration flow' })
export class FormQueries {}
